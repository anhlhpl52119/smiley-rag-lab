const response = await fetch('https://example.com');

const body = await response.text();
console.log("this is body and controller and my story",body);

function CreateNewTemplateForm () { 
 // write down your function to handle this issue when the controller is come and
}